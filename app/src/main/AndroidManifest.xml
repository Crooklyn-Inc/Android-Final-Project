<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".GeoCityList"></activity>
        <activity android:name=".DeezerSongSearch" />
        <activity android:name=".SongLyricsSearch" />
        <activity android:name=".SoccerMatchHighlights" />
        <activity android:name=".GeoDataSource" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Deezer_activity1" />
        <activity android:name=".Deezer_activity2" />
        <activity android:name=".DeezerSongSearch" />
    </application>

</manifest>